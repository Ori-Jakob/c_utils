#include "LinkedList.h";

LinkedList linkedListConstructor() {
	LinkedList list;

	list.head = NULL;
	list.tail = NULL;
	list.length = 0;

	return list;
}

void linkedListDeconstructor(LinkedList* list) {
	return NULL;
}
